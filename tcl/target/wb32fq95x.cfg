# SPDX-License-Identifier: GPL-2.0-or-later
#
# OpenOCD target configuration for Westberry WB32FQ95xx microcontrollers
#
# Copyright (C) 2026 Eric Molitor github.com/emolitor
#
# This is the target-only configuration. Use with an interface config:
#   openocd -f interface/cmsis-dap.cfg -f target/wb32fq95x.cfg
#
# Supported devices (WB32FQ95xx series, ARM Cortex-M3 r2p0):
#   WB32FQ95xC: 128-256 KB Flash, 12-36 KB SRAM
#
# Device specifications:
#   Flash: 0x08000000
#   SRAM:  0x20000000
#   Core:  ARM Cortex-M3 r2p0
#   SYS_ID: 0x40016400 (device identification)
#

#
# WB32FQ95xx devices support both JTAG and SWD transports.
#
source [find target/swj-dp.tcl]
source [find mem_helper.tcl]
source [find target/wb32_common.tcl]

if { [info exists CHIPNAME] } {
   set _CHIPNAME $CHIPNAME
} else {
   set _CHIPNAME wb32fq95x
}

set _ENDIAN little

# Work-area in RAM for flash programming
# Use 16KB (safe for all variants, smallest SRAM is 12 KB)
if { [info exists WORKAREASIZE] } {
   set _WORKAREASIZE $WORKAREASIZE
} else {
   set _WORKAREASIZE 0x4000
}

# SWD/JTAG TAP ID - Cortex-M3 r2p0
if { [info exists CPUTAPID] } {
   set _CPUTAPID $CPUTAPID
} else {
   if { [using_jtag] } {
      set _CPUTAPID 0x4ba00477
   } {
      set _CPUTAPID 0x2ba01477
   }
}

# Create the DAP
swj_newdap $_CHIPNAME cpu -irlen 4 -ircapture 0x1 -irmask 0xf -expected-id $_CPUTAPID
dap create $_CHIPNAME.dap -chain-position $_CHIPNAME.cpu

if {[using_jtag]} {
   jtag newtap $_CHIPNAME bs -irlen 5
}

set _TARGETNAME $_CHIPNAME.cpu
target create $_TARGETNAME cortex_m -endian $_ENDIAN -dap $_CHIPNAME.dap

$_TARGETNAME configure -work-area-phys 0x20000000 -work-area-size $_WORKAREASIZE -work-area-backup 0

# Flash bank configuration using custom WB32F10x driver
# The driver auto-detects flash size from SYS_MEMSZ register
# Note: The same wb32f10x driver supports both WB32F10x and WB32FQ95xx families
flash bank $_CHIPNAME.flash wb32f10x 0x08000000 0 0 0 $_TARGETNAME

# Adapter speed - start at 1MHz (F_CPU after reset is 8MHz MHSI, SWD <= F_CPU/6)
adapter speed 1000

adapter srst delay 100
if {[using_jtag]} {
   jtag_ntrst_delay 100
}

reset_config srst_nogate

if {![using_hla]} {
   # if srst is not fitted use SYSRESETREQ to
   # perform a soft reset
   cortex_m reset_config sysresetreq
}

$_TARGETNAME configure -event examine-end {
	# Enable debug during low power modes and stop watchdog
	# counters during halt. DBGMCU_CR is at 0xE0042004.
	# DBG_IWDG_STOP | DBG_WWDG_STOP | DBG_STANDBY | DBG_STOP | DBG_SLEEP
	mmw 0xE0042004 0x00000307 0
}

# Reset clock is 8MHz MHSI — drop speed back before reset
$_TARGETNAME configure -event reset-start {
	adapter speed 1000
}

# Increase speed after successful reset-init
$_TARGETNAME configure -event reset-init {
	adapter speed 4000
}
